[project]
name = "ks-baseline"
version = "0.1.0"
description = "Canonical baseline solution adapter"
requires-python = ">=3.12"
dependencies = [
    "ks-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
