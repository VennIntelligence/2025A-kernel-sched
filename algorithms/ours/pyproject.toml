[project]
name = "ks-ours"
version = "0.1.0"
description = "Promoted kernel-scheduling solver (re-exports ks_core.solver)"
requires-python = ">=3.12"
dependencies = [
    "ks-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
