# Iter 000: baseline01

Baseline reference rows copied from:

```text
results/exp001_baseline01/metrics.json
```

This iteration is not a runnable AutoResearch solver. It exists only to anchor
ledger comparisons against the canonical baseline.
